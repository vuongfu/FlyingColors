@model IEnumerable<TutorOnline.Web.Models.QuestionViewModels>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Bài tập của khóa học</title>
</head>
<body>
    <div class="container-fluid">
    @foreach (var item in Model)
    {
        <div class="row">
            <div class="col-md-12">
                <h4>Link tham khảo bài tập: </h4><a href="@Html.DisplayFor(modelItem => item.Content)">@Html.DisplayFor(modelItem => item.Content)</a>
            </div>
        </div>
        <div class="row">
            <div class="col-md-12">
                <h4>Link tham khảo bài tập: </h4><a href="@Html.DisplayFor(modelItem => item.Content)">@Html.DisplayFor(modelItem => item.Content)</a>
            </div>
        </div>
    }
    </div>
</body>
</html>
